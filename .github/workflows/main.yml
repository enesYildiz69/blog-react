name: Create Test File
on:
  push:
    branches:
      - main
jobs:
  create-file:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Create Test File
        run: touch test0.txt
